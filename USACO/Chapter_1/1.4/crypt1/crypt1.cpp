/*
ID: adiddee1
LANG: C++11
TASK: crypt1
*/
#include <fstream>
#include <algorithm>

using namespace std;

int main()
{
    ifstream in("crypt1.in");
    ofstream out("crypt1.out");
    int n;
    in >> n;
    int array[n];
    for(int i = 0; i < n; i++)
    {
        in >> array[i];
    }
    for(a : array)
    {
       for(b : array)
       {
           for(c:array)
           {
               for(d:array)
               {
                   for(e:array)
                   {
                        
                   }
               }
           }
       }
        
    }
}