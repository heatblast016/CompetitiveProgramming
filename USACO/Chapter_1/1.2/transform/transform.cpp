#include <iostream>
#include <fstream>
#include <cstdio>
using namespace std;
char[][] rotate(char matrix[] [], int size);
bool compare(char m1[][], char m2[][], int size);

int main()
{
    ifstream in("transform.in");
    ofstream out("transform.out");
    int size;
    in >> size;
    char matrix[size] [size];
}